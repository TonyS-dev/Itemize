version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        user: laravel
        uid: 1000
    image: itemize-prod
    container_name: itemize-app
    restart: always
    working_dir: /var/www
    # No volume mount: use the code built into the image
    env_file: .env.production
    environment:
      APP_ENV: production
      APP_DEBUG: 'false'
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy_network"
      - "traefik.http.routers.itemize.rule=Host(`itemize.tonys-dev.com`)"
      - "traefik.http.routers.itemize.entrypoints=websecure"
      - "traefik.http.routers.itemize.tls.certresolver=myresolver"
      - "traefik.http.services.itemize.loadbalancer.server.port=8000"
    depends_on:
      - db
    networks:
      - proxy_network
      - default

  db:
    image: postgres:15
    container_name: itemize-db
    restart: always
    environment:
      POSTGRES_DB: ${DB_DATABASE}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db-prod-data:/var/lib/postgresql/data
    env_file: .env.production
    networks:
      - default

volumes:
  db-prod-data:


networks:
  proxy_network:
    external: true
